"use server";

const scanResultAction = (decodedText, decodedResult) => {
  console.log(decodedText);

  //   return (
  //     <div>scanResultAction</div>
  //   )
};

export default scanResultAction;
